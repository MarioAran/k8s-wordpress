# k8s/wordpress/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: wordpress-service
  namespace: wordpress-stack
spec: 
  type: ClusterIP  # ← ClusterIP, no NodePort
  selector: 
    app: wordpress
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP